name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  linux:
    if: "!startsWith(github.ref, 'refs/tags/')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: make

      - name: Test (full run-all suite)
        run: make test

      - name: Sanitizers
        run: |
          make clean
          make debug
          bash tests/run.sh
          bash tests/adversarial/run.sh

      - name: Cross-backend diff
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: bash tests/test_transpiler_diff.sh

      - name: Fuzz (short budget)
        run: |
          sudo apt-get update -qq && sudo apt-get install -y clang
          make fuzz-parser FUZZ_CC=clang
          mkdir -p corpus
          cp examples/*.xs corpus/ 2>/dev/null || true
          cp tests/conformance/*.xs corpus/ 2>/dev/null || true
          ./fuzz_parser -max_total_time=60 -timeout=5 corpus/

  macos:
    if: "!startsWith(github.ref, 'refs/tags/')"
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: make
      - run: make test

  windows:
    if: "!startsWith(github.ref, 'refs/tags/')"
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          install: mingw-w64-x86_64-gcc make diffutils
      - name: Build
        shell: msys2 {0}
        run: make
      - name: Test
        shell: msys2 {0}
        run: make test

  wasm:
    if: "!startsWith(github.ref, 'refs/tags/')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install wasi-sdk
        run: |
          WASI_VERSION_FULL=25.0
          curl -sL https://github.com/WebAssembly/wasi-sdk/releases/download/wasi-sdk-25/wasi-sdk-${WASI_VERSION_FULL}-x86_64-linux.tar.gz | tar xz
          sudo mv wasi-sdk-${WASI_VERSION_FULL}-x86_64-linux /opt/wasi-sdk
      - name: Install wasmtime
        run: |
          curl -sL https://github.com/bytecodealliance/wasmtime/releases/download/v25.0.0/wasmtime-v25.0.0-x86_64-linux.tar.xz | tar xJ
          sudo cp wasmtime-v25.0.0-x86_64-linux/wasmtime /usr/local/bin/
      - name: Build WASM
        run: make wasm
      - name: Build native xs (needed for test orchestration helpers)
        run: make
      - name: Conformance suite through wasm
        run: |
          # Skip tests that need features the WASI build can't provide:
          # generators and async both rely on pthread primitives, and
          # wasi-emulated-pthread traps on cond_wait. Skip until we have
          # a single-threaded scheduler.
          skip="c03_functions c07_generators c14_async"
          pass=0; fail=0; skipped=0
          for f in tests/conformance/*.xs; do
              name=$(basename "$f" .xs)
              for s in $skip; do
                  if [ "$name" = "$s" ]; then
                      skipped=$((skipped + 1))
                      echo "  skip  $name (wasm: threading unsupported)"
                      continue 2
                  fi
              done
              out=$(wasmtime run --dir . xs.wasm -- --interp "$f" 2>&1); rc=$?
              if [ $rc -eq 0 ] && echo "$out" | grep -q "CONFORMANCE OK"; then
                  pass=$((pass + 1))
                  echo "  ok    $name (wasm)"
              else
                  fail=$((fail + 1))
                  echo "  FAIL  $name (wasm)"
                  echo "$out" | sed 's/^/        /'
              fi
          done
          echo "[wasm-conformance] $pass passed, $fail failed, $skipped skipped"
          [ $fail -eq 0 ]
      - name: Regression corpus through wasm
        run: |
          # Same threading caveat as conformance: bugs that exercise
          # generators or concurrent spawn can't run on wasi-pthread.
          skip="bug002_yield_outside_generator bug018_real_concurrent_spawn bug021_lazy_generators"
          pass=0; fail=0; skipped=0
          for f in tests/regression/*.xs; do
              name=$(basename "$f" .xs)
              for s in $skip; do
                  if [ "$name" = "$s" ]; then
                      skipped=$((skipped + 1))
                      echo "  skip  $name (wasm: threading unsupported)"
                      continue 2
                  fi
              done
              out=$(wasmtime run --dir . xs.wasm -- --interp "$f" 2>&1); rc=$?
              if [ $rc -eq 0 ]; then
                  pass=$((pass + 1))
                  echo "  ok    $name (wasm)"
              else
                  fail=$((fail + 1))
                  echo "  FAIL  $name (wasm)"
                  echo "$out" | sed 's/^/        /'
              fi
          done
          echo "[wasm-regression] $pass passed, $fail failed, $skipped skipped"
          [ $fail -eq 0 ]

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: xs-linux-x86_64
          - os: macos-latest
            name: xs-macos-x86_64
          - os: windows-latest
            name: xs-windows-x86_64.exe
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install MinGW (Windows)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          install: mingw-w64-x86_64-gcc make

      - name: Build release (Unix)
        if: runner.os != 'Windows'
        run: make release

      - name: Build release (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: make release

      - name: Rename binary (Linux/macOS)
        if: runner.os != 'Windows'
        run: mv xs ${{ matrix.name }}

      - name: Rename binary (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: mv xs.exe ${{ matrix.name }}

      - name: Zip Windows binary
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path ${{ matrix.name }} -DestinationPath xs-windows-x86_64.zip

      - name: Generate SHA256 checksums (Unix)
        if: runner.os != 'Windows'
        run: shasum -a 256 ${{ matrix.name }} > ${{ matrix.name }}.sha256

      - name: Generate SHA256 checksums (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          sha256sum ${{ matrix.name }} > ${{ matrix.name }}.sha256
          sha256sum xs-windows-x86_64.zip > xs-windows-x86_64.zip.sha256

      - name: Upload to release (Unix)
        if: runner.os != 'Windows'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ matrix.name }}
            ${{ matrix.name }}.sha256

      - name: Upload to release (Windows)
        if: runner.os == 'Windows'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ matrix.name }}
            ${{ matrix.name }}.sha256
            xs-windows-x86_64.zip
            xs-windows-x86_64.zip.sha256

  release-wasm:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Install wasi-sdk
        run: |
          WASI_VERSION_FULL=25.0
          curl -sL https://github.com/WebAssembly/wasi-sdk/releases/download/wasi-sdk-25/wasi-sdk-${WASI_VERSION_FULL}-x86_64-linux.tar.gz | tar xz
          sudo mv wasi-sdk-${WASI_VERSION_FULL}-x86_64-linux /opt/wasi-sdk
      - name: Build WASM
        run: make wasm
      - name: Generate SHA256 for WASM
        run: sha256sum xs.wasm > xs.wasm.sha256
      - name: Upload WASM to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            xs.wasm
            xs.wasm.sha256
      - name: Notify static repo
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/xs-lang0/static/dispatches \
            -d '{"event_type":"release"}' || true
